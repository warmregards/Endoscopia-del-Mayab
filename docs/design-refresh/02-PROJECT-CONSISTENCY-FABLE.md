Work in /Users/sanel/dev/personal/endoscopia-del-mayab-design. Preserve all existing uncommitted work. Read DESIGN-GUIDE.md, PROCEDURE-MIGRATION-PROMPT.md, MIGRATION-REPORT.md and the addendum at the top of MIGRATION-REVIEW.md in docs/design-refresh. The review addendum supersedes its earlier verdict. These instructions override historical expansive QA requirements.

Independently review the complete migrated project. Read REMAINING-MIGRATION-REPORT.md and verify its route inventory against actual routes. Report claims are not proof. Do not edit application code; write only the review report.

Prioritize high/medium defects: missing routes, lost content/SEO/LP directives, broken forms/links/tracking hooks, unreadable contrast, hidden/clipped text, price-container overflow, mobile CTA obstruction, regressions in shared layout or keyboard/reduced-motion behavior. Recheck the earlier contact contrast and tablet price failure patterns. Measure prices against their own padded containers, not only document width. Composite alpha colors correctly; do not flag unsupported oklab parsing as measured contrast failure.

Inspect all public routes at 390px. Inspect one representative per distinct layout/template at 1280px, and each exceptional route. Measure display prices at 320/640/768/1024 where relevant. Check long headings, tables and calendar edge fit. Expand only when a confirmed issue or shared change warrants it. Compare home, endoscopy and colonoscopy shared sections where changed. Verify sticky bars at top/mid/closing/return and section jumps; verify reduced motion. Do not submit forms or follow contact CTAs.

Use stored before/after baselines to verify content, headings, links, IDs, metadata, schema, robots and canonical parity. Inspect source for analytics/form changes that text snapshots miss. If a baseline is missing, report the limitation instead of manufacturing one. Run git diff --check and a typecheck only if no trustworthy final result exists or relevant code changed afterward. Avoid repeated screenshot suites, infrastructure and unrelated fixes.

Write docs/design-refresh/PROJECT-CONSISTENCY-REVIEW.md with ready / ready with minor issues / needs fixes verdict; route coverage; prioritized confirmed findings with file/line, viewport, evidence, user impact and bounded fix; intentional exceptions; accepted minor issues; checks run and uncertainty. Distinguish old issues from regressions. High/medium unresolved defects block release recommendation; low issues do not automatically block. Do not fix code during this independent review.
