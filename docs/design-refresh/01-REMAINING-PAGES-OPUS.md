Work in /Users/sanel/dev/personal/endoscopia-del-mayab-design. Preserve all existing uncommitted work. Read DESIGN-GUIDE.md, PROCEDURE-MIGRATION-PROMPT.md, MIGRATION-REPORT.md and the addendum at the top of MIGRATION-REVIEW.md in docs/design-refresh. The review addendum supersedes its earlier verdict. These instructions override historical expansive QA requirements.

Migrate all remaining public, user-facing pages to the established design. First enumerate actual Next.js routes, including dynamic route generators and route groups. Create a concise route manifest identifying already migrated, remaining, and excluded routes. Infer remaining status from source/design opt-in and reports, not git status alone. Exclude API endpoints, redirects, error/framework pages and non-public tools. Preserve dedicated legal/article/utility/advertising layouts; do not force them into a procedure template. Identify uncertain scope explicitly and continue unambiguous pages.

Implement in sequential groups of similar pages. Do one representative page, verify it, then reuse its composition. Finish every remaining in-scope route; record blocked routes honestly. Do not spawn agents or concurrent shared-file writers unless separately authorized.

Reuse design-system.module.css and design-pages.module.css roles. Add a scoped reusable role only when existing roles cannot express a repeated requirement. Do not copy entire procedure stylesheets per route or refactor unrelated code. Keep all existing content, medical claims, prices/data sources, headings, links, anchors, metadata, schema, robots directives, analytics, and form behavior unchanged. Preserve LP noindex settings and separate conversion layout. Never replace missing procedure information with another procedure's content.

Constraints learned from review:
- Dark bands require explicit readable inverse text, including paragraphs, captions, links and icon labels. Do not assume text-primary-foreground is light under local tokens. Preserve the contacto closingText fix.
- Display prices must fit their container's padded inner width at 320/390/640/768/1024/1280px. No mid-value wraps, overlap with arrows, or gutter encroachment. No horizontal page scroll is insufficient evidence. Adjust typography/grid/padding, not text, price data or truncation. Inline prose price wrapping remains accepted.
- Keep mobile CTAs prominent, icons beside associated text, and appropriate touch targets. Preserve emergency maroon and dark-band pale CTAs.
- Reuse sticky visibility hooks and route handling; verify top, middle, closing, return-to-top and a direct section jump. Preserve LP-specific sticky behavior. Do not replace functioning fallback behavior without evidence.
- Reduced motion must disable smooth scrolling and decorative movement. Keep disclosures semantic, keyboard focus visible, and calendar within viewport padding. No autofocus or test submissions.
- Existing accepted low-priority findings in the review are not release blockers and do not justify scope expansion; do not reproduce them on newly migrated pages.

Bounded QA: one content/SEO baseline per target before edits; reuse tooling and record artifact paths. No full before screenshot suites, deterministic-dump tests, repeated reference matrices or new testing framework. Inspect every new page at 390 and 1280px; use targeted screenshots only when useful. Measure display-price fit at the listed breakpoints where prices exist. Check 320 overflow and 768 layout. Compare baseline once after changes. Spot-check only affected reference sections for shared changes. One batch typecheck and git diff --check; repeat only after relevant fixes. Use the existing preview; no production build against active dev .next, dependency installs, commits, pushes or deployment.

Write docs/design-refresh/REMAINING-MIGRATION-REPORT.md: route manifest, per-route status/preview, composition used, changed files/shared blast radius, exact QA results, content/SEO differences, existing versus new errors, unverified behavior, known issues and artifact paths. Do not claim checks you did not run. Leave local preview ready for independent review.
